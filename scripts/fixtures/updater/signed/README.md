These tiny updater artifacts are test-only fixtures. Their signatures were made
with a disposable key whose private half was destroyed and was never used for a
release. Only the non-sensitive public keys and signatures are kept here.
