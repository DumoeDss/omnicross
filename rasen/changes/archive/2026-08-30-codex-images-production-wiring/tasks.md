## 1. Permissions, configuration, and atomic administration

- [x] 1.1 Add `OutboundPermission = OutboundEndpoint | 'images'` while keeping `OutboundEndpoint`, ingress formats, gateway bindings, endpoint configs, and text route maps four-member-only; centralize exact permission interpretation so an absent legacy list grants only the four legacy families and an explicit empty list denies all.
- [x] 1.2 Update named-key contracts, verification projections, create/list DTOs, and persistence typing to carry `OutboundPermission[]`; keep native integration-key creation exactly `responses/messages` and add compatibility tests for absent, empty, explicit, integration, revoked, and malformed stored rows.
- [x] 1.3 Add an exact `outboundApiKeysSetPermissions` mutation to the key DB port and `JsonOutboundKeyDb`, with strict pre-mutation validation, revoked/missing handling, and a single failure-atomic row write; test unknown values, duplicates, empty lists, and write failure without changing the effective prior policy.
- [x] 1.4 Add the authenticated key-permissions admin mutation and never-secret DTO projection, returning 400 for malformed or duplicate values and 404/conflict for missing or revoked keys without echoing key material.
- [x] 1.5 Add the nested Images config contracts, frozen hard ceilings, migration-safe defaults, and tolerant read normalization for enablement, provider/model aliases, account policy, queue/generation deadlines, API limits, temporary budgets, reference/state quotas, cleanup, storage, remote policy, and evidence TTL.
- [x] 1.6 Add strict admin-edge Images validation that rejects unknown members/providers, ambiguous account selection, invalid fallback/model aliases, non-integer or out-of-range values, inconsistent aggregate limits/TTLs, worktree-contained or symlink-escaping roots, and remote enablement without an injected proven resolver.
- [x] 1.7 Extend the settings store with atomic temp-write/fsync/rename replacement and a recoverable prior-document snapshot while preserving proxy/webhook/billing secret handling, UTF-8, unrelated config fields, and the existing missing/corrupt read behavior.
- [x] 1.8 Refactor `PUT /admin/api/server` through an explicit prepare/persist/publish/rollback coordinator; prepare all fallible Images/listener work, persist once, perform infallible snapshot swaps, and restore persisted/runtime state on injected persist or publish faults before sending a response.
- [x] 1.9 Add transaction tests covering Images-only and mixed patches, validation failure, runtime-prepare failure, persistence failure, injected commit failure, rollback failure reporting, secret preservation, no misleading applied audit, and successful coherence across `GET /server`, live status, and new requests.
- [x] 1.10 Extend config migration, import/export, CLI-safe types, and public exports so missing Images data remains default-disabled and no migration rewrites existing key permissions or grants Images implicitly.

## 2. Persistent stores and temporary-resource lifecycle

- [x] 2.1 Add a daemon image-path resolver that derives separate restrictive temporary, durable artifact/state, evidence, and mount-manifest roots from private application data; reject detected worktrees, unverified descendants, symlinks, broad roots, and caller/content-derived filenames on every destructive operation.
- [x] 2.2 Extend `ImageRequestResourceScope` with an optional shared budget port and tenant-aware scope ownership while preserving existing callers; charge active-scope, per-tenant, and total temporary bytes before write and release exactly on write rollback, spool disposal, or idempotent cleanup.
- [x] 2.3 Implement the daemon temporary budget/root factory and owned-directory marker, wire it through `createResourceScope`, and add concurrent JSON/multipart/spool tests for global/tenant exhaustion, cancellation, partial writes, successful cleanup, and zero workspace writes.
- [x] 2.4 Implement a versioned file-backed `ImageReferenceStore` with persistent domain-separated tenant HMAC salt, unpredictable IDs, exclusive restrictive files, streaming bounded writes/reads, atomic manifest transactions, and optional secret-box protection for necessary opaque provider references.
- [x] 2.5 Enforce reference per-artifact, per-tenant bytes, total bytes, and entry quotas before publication; implement lease pinning, owner-only expiry, cross-tenant/capacity not-found behavior, idempotent delete/release, and bounded tombstones with fake-clock and concurrent-writer tests.
- [x] 2.6 Implement a versioned file-backed `ResponsesImageStateStore` matching the existing in-memory semantics for ordered calls, explicit known-empty response markers, atomic multi-call commit, exact idempotency, tenant isolation, expiry, capacity eviction, tombstones, and leases.
- [x] 2.7 Persist and return evicted/expired call bindings from Responses state cleanup, and add a coordinator that best-effort deletes their image references without turning capacity eviction into owner-visible expiry or deleting actively leased artifacts.
- [x] 2.8 Implement the versioned storage-mount catalog so root changes direct new writes to a validated active backend while authorized unexpired IDs remain resolvable from retired mounts across hot reload and restart; atomically retire only verified empty mounts.
- [x] 2.9 Add bounded startup reconciliation for manifests, counters, incomplete transactions, orphan metadata/files, corrupt schemas, invalid descendants, stale owned temporary directories, and broken state/reference links without following symlinks or touching foreign directories.
- [x] 2.10 Add an unref'ed cleanup service with bounded passes and dependency ordering across state, references, tombstones, temporary directories, evidence, and retired mounts; expose counts/bytes only and make start/stop/reset idempotent.
- [x] 2.11 Add persistence fault-injection, restart, corrupt-file, symlink/race, quota, lease-versus-cleanup, storage-root migration, and secret-sentinel tests, including strict proof that known-empty Responses state survives restart.

## 3. Account-bound scheduling and capability evidence

- [x] 3.1 Add the stable `image_queue_full` and `image_queue_timeout` contract/error mappings, define a credential/content-blind image execution scheduler port, and add it optionally to `CodexSubscriptionImageProviderOptions`; invoke it after `applyHeaders(...reportSelection)` binds the account and evidence is resolved, but before candidate request construction or transport.
- [x] 3.2 Implement the daemon hierarchical scheduler with per-selected-account active limits, one bounded global waiting population, round-robin tenant queues, opaque HMAC account keys, snapshot configuration, and secret-free status.
- [x] 3.3 Implement distinct queue and generation clocks so queue timeout performs no upstream call and generation timeout begins only after grant; preserve the two stable outcomes through API and hosted execution.
- [x] 3.4 Make waiter cancellation, provider throw, failed terminal, iterator return, caller abort, timeout, hot-reload retirement, and shutdown release each scheduler grant exactly once; add deterministic race tests and prove image scheduling never consumes the generic text concurrency gate.
- [x] 3.5 Implement a versioned file-backed `CodexImageCapabilityEvidenceSource` keyed by selected-account HMAC with strict expiry and an allow-listed narrow schema; never persist raw account identity, credentials, request/response bodies, prompts, artifacts, Base64, or private wire fields.
- [x] 3.6 Add a subscriptions-owned live verifier that contains all candidate private-wire construction/parsing, makes at most one minimal low-quality PNG request through proxy-aware `fetchUpstream` with `redactBodies: true`, strictly decodes it, destroys it, and returns only narrowly observed evidence.
- [x] 3.7 Test capability intersection across adapter/account/upstream evidence, account mismatch, model/option mismatch, stale/failed evidence, current fail-closed defaults, auth refresh bound to the same selected account, and omission of unverified usage/moderation/cost/revised-prompt fields.

## 4. Runtime generations, registry composition, and outbound dispatch

- [x] 4.1 Implement `ImageRuntimeManager` with current/draining generations, reference-counted HTTP and hosted leases, stable forwarding handlers, prepare/publish/rollback/dispose operations, and a disabled generation that fails closed without constructing a provider call.
- [x] 4.2 Build each runtime generation from `subscriptionAccounts.getStrategy('codex')`, `createCodexSubscriptionImageProvider`, one `ImageProviderRegistry`, one `ImageOrchestrator`, the shared mounted reference/state stores, scheduler, evidence source, temporary factory, safe runtime resolver, and metadata-only observability.
- [x] 4.3 Derive tenant only from non-empty `route.apiKeyId`, apply configured model aliases/account or group hints, generate only keyed irreversible user fingerprints, and reject any runtime context that contains or derives identity from the raw inbound bearer.
- [x] 4.4 Create one `OpenAIOperationRegistry` and register exactly one stable `images.generate` and `images.edit` forwarding contribution before the first bootstrap `getProviderProxy` call; inject the same registry into the first `ProviderProxyDeps` construction and dispose it on daemon stop/reset.
- [x] 4.5 Add bootstrap ordering and repeated-reset tests proving no proxy is constructed before registry availability and no duplicate registration, stale generation, store handle, timer, scope, provider, or queue waiter survives rebuild or partial boot failure.
- [x] 4.6 Add early Images classification to the outbound router using the existing classifier, authorize the distinct Images permission after named-key/loopback/rate checks, and dispatch the original request stream through the registry-backed proxy path with a minimal trusted route.
- [x] 4.7 Ensure the Images branch returns before text binding resolution, model restriction, cost conversion, generic JSON read/replay, generic concurrency/serial queues, and text ingress; add chunked multipart and backpressure tests proving the contribution receives an unconsumed stream.
- [x] 4.8 Add pre-body rejection tests for missing/invalid keys, loopback violations, Responses-only/legacy keys, disabled runtime, absent handler, oversized uploads, and unknown routes, asserting zero temp files, account selection, queue entries, provider calls, or text fallback.
- [x] 4.9 Expose an acquireable hosted Responses image contribution factory from the daemon handle; pin one runtime generation until scope disposal/release, share its orchestrator/reference/state/evidence truth, and keep the factory dormant with no self-registration or ingress mutation.
- [x] 4.10 Add hosted-factory tests for no-acquisition dormancy, real generation pinning across hot reload, unavailable current production evidence, exact release on construction/execution failures, and the sibling handoff order using a synthetic final-integrator harness only.
- [x] 4.11 Add runtime update tests for enable/disable, provider/account/model/limits/deadlines/storage/remote changes, failed replacement construction, old queued/active/hosted work draining on its snapshot, and retirement only after the final lease.

## 5. Capability, models, audit, doctor, and UI

- [x] 5.1 Extend the normal audit capture API with an Images body-suppression option selected before `beginAuditCapture` installs response wrappers; keep metadata coverage while structurally preventing request stashing and response-body capture for JSON, multipart, Base64, and SSE Images traffic.
- [x] 5.2 Implement bounded low-cardinality image audit/telemetry aggregation for safe operation/model/provider/options, terminals, stable errors, queue/generation latency, input/output counts and bytes, reference/cleanup outcomes, and only actually known retry/auth-refresh values.
- [x] 5.3 Add sentinel tests across audit enabled/disabled/body-capture modes, logger, exceptions, traces, metrics, stores, status, doctor, UI DTOs, and snapshots for credentials, Cookies, prompts, URLs/query strings, filenames/paths, masks, images, Base64/data URLs, raw tenant/account IDs, hashes, and provider references.
- [x] 5.4 Project `gpt-image-2` into OpenAI `/v1/models` only for a key with explicit Images permission plus enabled config and fresh effective evidence; keep legacy/Responses-only/disabled/stale cases absent and deduplicate it from text catalogs.
- [x] 5.5 Change auto model-list shape selection so any explicitly Images-authorized key receives OpenAI shape, while a forced Anthropic shape remains valid and omits the image model; add mixed Messages+Images and four-endpoint regression tests.
- [x] 5.6 Add an authenticated Images admin capability/status handler backed by narrow runtime-manager readers, returning configured versus effective state, safe reason/evidence age, affirmed feature/limit matrix, generation, additive endpoint URLs, bounded queue/temp/store utilization, and draining counts without probing.
- [x] 5.7 Extend outbound/admin status with additive optional Images URLs and runtime summaries without changing the meanings or required shape of the existing four text endpoints/formats; add compatibility tests for older UI consumers and disabled Images.
- [x] 5.8 Add `omnicross doctor images` local-only checks for normalized config, roots, stores, permission schema, account presence, and cached evidence, and prove it makes zero image upstream calls.
- [x] 5.9 Add the explicitly consuming `doctor images --live` flow with pre-call quota warning, enablement/account preconditions, one verifier call, narrow evidence persistence only after strict success, artifact/temp cleanup, and stable safe failure diagnostics.
- [x] 5.10 Extend UI daemon types and adapters for Images config, exact key permissions, capability/status, URLs, queue/temp/store utilization, and config mutations; keep `OutboundEndpointId` four-member-only and introduce a separate permission type.
- [x] 5.11 Add separate Responses and Images key controls, Images configuration/status cards, configured-versus-effective messaging, safe account selectors, unsupported feature matrix, remote/storage warnings, loading/error/rollback behavior, and translations in the API Service UI.
- [x] 5.12 Add UI component/hook/adapter tests for permission exactness, legacy keys, failed mutation rollback, strict normalized config echo, unavailable/stale/fresh capability states, non-consuming polling, additive status compatibility, and absence of secret/path fields.

## 6. Daemon, SDK, and integration verification

- [x] 6.1 Add a labeled synthetic verified-provider injection seam available only to tests and use it to boot the real daemon composition with the production registry, router, runtime manager, scheduler, temp budget, stores, audit suppression, and cleanup services.
- [x] 6.2 Add current JavaScript OpenAI SDK end-to-end tests for authenticated generate, JSON edit/reference, multipart edit/mask, stream/non-stream mapping, errors, cancellation, model listing, tenant isolation, persistence, and cleanup through the bound outbound listener.
- [x] 6.3 Add pinned Python OpenAI SDK contract tests for generate and multipart edit/mask through the same booted daemon, with subprocess timeout, isolated temporary home/config, exact response parsing, cancellation/failure coverage where supported, and no repository-relative artifacts.
- [x] 6.4 Add daemon boot/start/stop/reset and hot-reload E2E tests covering default-disabled behavior, exact key permission changes, first-proxy registry injection, selected-account scheduling, persistent restart references/state, atomic config rollback, and metadata-only status/audit.
- [x] 6.5 Run focused foundation, Images API, hosted Responses contribution, adapter/private-wire, permission/router/models, config/admin, persistence/lifecycle, doctor, UI, and SDK suites; fix only regressions attributable to this Change.
- [x] 6.6 Run the existing Chat, Responses, Messages, Gemini, native integration-key, route lease, key policy, audit, billing, proxy, subscription-account, bootstrap, UI adapter, and public-export regression slices affected by the wiring.
- [x] 6.7 Run package typechecks and builds for contracts, core, subscriptions, daemon, and UI, followed by the repository-required full verification command once the focused suites are stable.

## 7. Security gates, live evidence, and delivery handoff

- [x] 7.1 Add negative security tests for traversal, symlink swaps, worktree roots, foreign temp directories, decompression/pixel bombs, oversized JSON/multipart/Base64, queue/temp/store exhaustion, cross-tenant references/state, stale evidence, SSRF/redirect/DNS rebinding, header abuse, and cleanup races.
- [x] 7.2 Keep remote URLs disabled in production unless the composed resolver proves per-hop DNS/address policy, connection pinning, redirect revalidation, proxy behavior, byte/header/time limits, and common raster validation; record disabled/unsupported rather than weakening the gate.
- [x] 7.3 Run secret scans over authored source, fixtures, generated evidence, logs, snapshots, stores, config, audit, and diagnostics; verify no real credential, Cookie, raw account ID, prompt, image/Base64, provider reference, or machine-specific storage path is committed.
- [x] 7.4 Run strict UTF-8 decoding, BOM/U+FFFD/mojibake checks, JSON/TOML/YAML parsing, `git diff --check`, generated-file inspection, and a scoped diff review that preserves unrelated user changes.
- [x] 7.5 Run a forbidden-path/content gate proving zero authored changes under `packages/core/src/openai-operation/**`, every `openaiResponsesIngress.ts`, `providerProxyShared.ts`, Native Responses core/profile/affinity, and excluded web-search/compact/Responses-WS/Files/stored/background features.
- [x] 7.6 If explicit live credentials and approval are present, run `doctor images --live` once and retain only sanitized Tier B metadata; otherwise record not-run/unverified without blocking deterministic Tier A verification or claiming production capability.
- [x] 7.7 If an eligible current Codex binary and explicit opt-in are present, run the isolated temporary-home custom-provider `$imagegen` gate without a script fallback; record tool absence, unavailable hosted capability, or missing final ingress integration honestly as unsupported Tier C.
- [x] 7.8 Write apply and verification evidence with baseline/HEAD, exact commands/results, failure-atomic update proof, storage/scheduler/lifecycle proof, SDK versions, secret and forbidden-path gates, and a PR-ready Tier A/B/C capability matrix covering every supported, unsupported, unavailable, and unverified row.
- [x] 7.9 Document the exported hosted factory and the final integrator's exact affinity-authorize → acquire generation → inspect/validate selection → create scope → execute with shared allocator → commit → assemble official terminal → dispose → release order; explicitly assign selector, allocator, affinity, terminal assembly, and ingress injection outside this Change.
- [x] 7.10 Append one to three durable implementation discoveries to the parent planning context, verify all proposal capabilities have matching specs, run `rasen validate codex-images-production-wiring --type change --strict`, and hand the locally complete child back without modifying parent run-state, auto-run, portfolio state, or pushing.
