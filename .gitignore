node_modules/
/apps/desktop/src-tauri/*.key
/apps/desktop/src-tauri/*.key.password
dist/
*.log
.DS_Store
coverage/
*.tsbuildinfo
scripts/updater-signature-verifier/target/
omnicross.dev.config.json
# Daemon runtime state written next to the config file (so a repo-root dev
# config drops these here) — generated, refreshed on every pricing sync.
pricing.json
pricing-refresh.json

# Local agent tooling and embedded workflow workspace
/.claude/
/rasen/
/.codex/
/.rasen/
/.ua/
